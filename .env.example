# This frontend's own public URL, used for canonical links & structured data
VITE_SITE_URL=https://gemcitycleaningcrew.com

# Optional
VITE_TWITTER_HANDLE=

# GoHighLevel media-upload token used by scripts/ghl-upload-media.js (Node CLI only —
# never prefix with VITE_, it must not be bundled into the client build)
GHL_TOKEN=
